# 🚀 Quick Start Guide

Быстрое руководство по запуску и использованию Blog API.

## Запуск проекта (5 минут)

### 1. Активируйте виртуальное окружение

```bash
cd /home/user/Desktop/test.kg
source venv/bin/activate
```

### 2. Запустите сервер

```bash
python manage.py runserver
```

✅ Сервер запущен на: **http://127.0.0.1:8000/**

## Работа с API (через Swagger UI)

### Шаг 1: Откройте документацию

🔗 **http://127.0.0.1:8000/api/docs/**

### Шаг 2: Зарегистрируйте пользователя

1. Найдите секцию **"blog_api"**
2. Откройте `POST /api/v1/users/`
3. Нажмите **"Try it out"**
4. Введите данные:

```json
{
  "username": "myuser",
  "email": "myuser@example.com",
  "password": "mypassword123"
}
```

5. Нажмите **"Execute"**

### Шаг 3: Получите JWT токен

1. Найдите `POST /api/v1/auth/token/`
2. Нажмите **"Try it out"**
3. Введите данные из шага 2:

```json
{
  "username": "myuser",
  "password": "mypassword123"
}
```

4. Нажмите **"Execute"**
5. **Скопируйте значение `access`** из ответа

### Шаг 4: Авторизуйтесь

1. Нажмите кнопку **"Authorize"** 🔒 в правом верхнем углу
2. Введите: `Bearer ВАШ_ACCESS_TOKEN` (замените на токен из шага 3)
3. Нажмите **"Authorize"**
4. Нажмите **"Close"**

### Шаг 5: Создайте пост

1. Найдите `POST /api/v1/posts/`
2. Нажмите **"Try it out"**
3. Введите данные:

```json
{
  "title": "Мой первый пост",
  "body": "Это содержимое моего первого поста в блоге!",
  "is_published": true
}
```

4. Нажмите **"Execute"**

### Шаг 6: Просмотрите посты

1. Найдите `GET /api/v1/posts/`
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**

Вы увидите список всех постов (включая только что созданный).

### Шаг 7: Добавьте комментарий

1. Найдите `POST /api/v1/posts/{id}/comments/`
2. Нажмите **"Try it out"**
3. Введите **id** вашего поста (например, `1`)
4. Введите комментарий:

```json
{
  "body": "Отличный пост! Спасибо за информацию."
}
```

5. Нажмите **"Execute"**

## Альтернатива: Работа через curl

### Регистрация

```bash
curl -X POST http://127.0.0.1:8000/api/v1/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "testpass123"
  }'
```

### Получение токена

```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/token/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "testpass123"
  }'
```

Сохраните `access` токен из ответа.

### Создание поста

```bash
curl -X POST http://127.0.0.1:8000/api/v1/posts/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "title": "Test Post",
    "body": "Post content",
    "is_published": true
  }'
```

Замените `YOUR_ACCESS_TOKEN` на ваш токен.

### Просмотр постов

```bash
# Без авторизации (только опубликованные)
curl http://127.0.0.1:8000/api/v1/posts/

# С авторизацией (все посты)
curl http://127.0.0.1:8000/api/v1/posts/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## Основные URL

| Назначение | URL |
|-----------|-----|
| Swagger UI | http://127.0.0.1:8000/api/docs/ |
| ReDoc | http://127.0.0.1:8000/api/redoc/ |
| API Schema | http://127.0.0.1:8000/api/schema/ |
| Admin Panel | http://127.0.0.1:8000/admin/ |

## Основные эндпоинты

| Метод | Эндпоинт | Описание | Доступ |
|-------|----------|----------|--------|
| POST | /api/v1/users/ | Регистрация | Все |
| POST | /api/v1/auth/token/ | Получить токен | Все |
| GET | /api/v1/posts/ | Список постов | Все* |
| POST | /api/v1/posts/ | Создать пост | Auth |
| GET | /api/v1/posts/{id}/ | Детали поста | Все* |
| PUT | /api/v1/posts/{id}/ | Обновить пост | Автор |
| DELETE | /api/v1/posts/{id}/ | Удалить пост | Автор |
| POST | /api/v1/posts/{id}/comments/ | Добавить комментарий | Auth |
| GET | /api/v1/posts/{id}/comments/ | Список комментариев | Все |

**Примечание***: Гости видят только опубликованные посты (is_published=true)

## Права доступа

```
┌──────────────┬──────────┬───────────────┬──────┐
│   Действие   │  Гость   │ Авторизованный│Автор │
├──────────────┼──────────┼───────────────┼──────┤
│ Регистрация  │    ✅    │      ✅       │  ✅  │
│ Получить токен│   ✅    │      ✅       │  ✅  │
│ Список постов│ ✅ (pub) │   ✅ (all)    │  ✅  │
│ Создать пост │    ❌    │      ✅       │  ✅  │
│ Читать пост  │ ✅ (pub) │   ✅ (all)    │  ✅  │
│ Изменить пост│    ❌    │      ❌       │  ✅  │
│ Удалить пост │    ❌    │      ❌       │  ✅  │
│ Добавить комм│    ❌    │      ✅       │  ✅  │
└──────────────┴──────────┴───────────────┴──────┘

(pub) = только опубликованные
(all) = все посты
```

## Пагинация

API автоматически разбивает результаты на страницы:
- По умолчанию: **10 элементов на страницу**
- Параметр: `?page=2` для следующей страницы

Пример ответа:
```json
{
  "count": 25,
  "next": "http://127.0.0.1:8000/api/v1/posts/?page=2",
  "previous": null,
  "results": [...]
}
```

## Troubleshooting

### Сервер не запускается

```bash
# Проверьте, что виртуальное окружение активировано
which python
# Должно показать: /path/to/venv/bin/python

# Проверьте установленные пакеты
pip list

# Переустановите зависимости
pip install -r requirements.txt

# Примените миграции
python manage.py migrate
```

### Ошибка "Token is invalid or expired"

Токен истек (срок действия: 1 час). Получите новый:

```bash
# Используя refresh токен
curl -X POST http://127.0.0.1:8000/api/v1/auth/token/refresh/ \
  -H "Content-Type: application/json" \
  -d '{"refresh": "YOUR_REFRESH_TOKEN"}'

# Или получите новый токен
curl -X POST http://127.0.0.1:8000/api/v1/auth/token/ \
  -H "Content-Type: application/json" \
  -d '{"username": "...", "password": "..."}'
```

### Ошибка 403 Forbidden

Вы пытаетесь изменить/удалить чужой контент. Только автор может редактировать свои посты/комментарии.

### Swagger UI не загружается

1. Проверьте, что сервер запущен: http://127.0.0.1:8000/
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Проверьте консоль браузера на ошибки (F12)

## Следующие шаги

1. 📖 Прочитайте [README.md](README.md) для полной документации
2. ⚙️ Изучите [API_DOCUMENTATION_SETUP.md](API_DOCUMENTATION_SETUP.md) для настройки документации
3. 🧪 Экспериментируйте с API через Swagger UI
4. 📊 Изучите структуру проекта в [README.md](README.md#структура-проекта)

---

**Полезно? Начните работу с API прямо сейчас!** 🎉

Swagger UI: http://127.0.0.1:8000/api/docs/
